using UnityEngine;

public class VisionZone : MonoBehaviour
{
    public float feather = 0.1f;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
    }
}
